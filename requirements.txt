# The tool itself only needs the Python standard library.
#
# Optional extras (install what you need):
#
#   AI image matching (ResNet-18, best quality):
#     pip install torch torchvision Pillow numpy
#     Tip: on CPU-only machines, save disk space with:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
#   Lightweight matching (perceptual hash, tiny download):
#     pip install Pillow ImageHash
#
#   Progress bars:
#     pip install tqdm
#
# This file installs everything needed for full AI matching (-r / --reference):
torch
torchvision
Pillow
numpy
